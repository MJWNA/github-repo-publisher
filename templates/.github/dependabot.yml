version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      github-actions:
        patterns:
          - "*"
  # Add only ecosystems that exist in the repo.
  # - package-ecosystem: "npm"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
  #   groups:
  #     npm-minor-patch:
  #       update-types:
  #         - "minor"
  #         - "patch"
